<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidproject">

    <application
        android:name=".activities.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidProject">
        <activity android:name=".activities.RisksActivity"></activity>
        <activity android:name=".activities.StatisticsActivity"></activity>
        <activity android:name=".activities.AlcoholActivity"></activity>
        <activity android:name=".activities.MovementActivity"></activity>
        <activity android:name=".activities.AddTobaccoActivity"></activity>
        <activity android:name=".activities.GenderChooseActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>


</manifest>